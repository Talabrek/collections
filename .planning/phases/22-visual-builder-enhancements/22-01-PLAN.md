---
phase: 22-visual-builder-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/web/js/app.js
  - src/main/resources/web/index.html
  - src/main/resources/web/css/admin.css
autonomous: true

must_haves:
  truths:
    - "Admin can select from 6 template types when creating new collection"
    - "Selecting template pre-fills form with appropriate biomes, dimensions, Y-levels"
    - "Template items are added to collection with placeholder names"
    - "Admin can still start blank collection without template"
  artifacts:
    - path: "src/main/resources/web/js/app.js"
      provides: "Template data object and loadTemplate function"
      contains: "const templates = {"
    - path: "src/main/resources/web/index.html"
      provides: "Template selector UI"
      contains: "template-selector"
    - path: "src/main/resources/web/css/admin.css"
      provides: "Template button styling"
      contains: ".template-btn"
  key_links:
    - from: "index.html"
      to: "app.js loadTemplate()"
      via: "template button click handlers"
      pattern: "data-template=.*loadTemplate"
---

<objective>
Add collection templates so admin can quickly start new collections with pre-configured biomes, dimensions, and sample items for common environments (forest, ocean, nether, cave, end, desert).

Purpose: Reduces setup time from minutes to seconds when creating new collections for common biome types.
Output: Template selector UI that appears when clicking "New Collection", with 6 template buttons plus "Start Blank" option.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-visual-builder-enhancements/22-RESEARCH.md

@src/main/resources/web/index.html
@src/main/resources/web/js/app.js
@src/main/resources/web/css/admin.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add template data and loadTemplate function to app.js</name>
  <files>src/main/resources/web/js/app.js</files>
  <action>
Add a `templates` object at the top of app.js (after the state variables) containing 6 template configurations:

```javascript
const templates = {
  forest: {
    name: "Forest Collection",
    tier: "COMMON",
    biomes: ["FOREST", "BIRCH_FOREST", "OLD_GROWTH_BIRCH_FOREST", "FLOWER_FOREST"],
    dimensions: ["NORMAL"],
    minY: 60,
    maxY: 100,
    items: [
      { id: "forest_item_1", name: "Forest Item 1", material: "OAK_LOG", weight: 25 },
      { id: "forest_item_2", name: "Forest Item 2", material: "MOSS_BLOCK", weight: 25 },
      { id: "forest_item_3", name: "Forest Item 3", material: "FERN", weight: 25 },
      { id: "forest_item_4", name: "Forest Item 4", material: "SWEET_BERRIES", weight: 25 }
    ],
    experience: 150
  },
  ocean: {
    name: "Ocean Collection",
    tier: "COMMON",
    biomes: ["OCEAN", "DEEP_OCEAN", "WARM_OCEAN", "LUKEWARM_OCEAN", "COLD_OCEAN"],
    dimensions: ["NORMAL"],
    minY: 20,
    maxY: 62,
    items: [
      { id: "ocean_item_1", name: "Ocean Item 1", material: "PRISMARINE_SHARD", weight: 25 },
      { id: "ocean_item_2", name: "Ocean Item 2", material: "SEA_PICKLE", weight: 25 },
      { id: "ocean_item_3", name: "Ocean Item 3", material: "KELP", weight: 25 },
      { id: "ocean_item_4", name: "Ocean Item 4", material: "NAUTILUS_SHELL", weight: 25 }
    ],
    experience: 175
  },
  nether: {
    name: "Nether Collection",
    tier: "UNCOMMON",
    biomes: ["NETHER_WASTES", "CRIMSON_FOREST", "WARPED_FOREST", "SOUL_SAND_VALLEY", "BASALT_DELTAS"],
    dimensions: ["NETHER"],
    minY: 30,
    maxY: 120,
    items: [
      { id: "nether_item_1", name: "Nether Item 1", material: "NETHER_WART", weight: 25 },
      { id: "nether_item_2", name: "Nether Item 2", material: "GLOWSTONE_DUST", weight: 25 },
      { id: "nether_item_3", name: "Nether Item 3", material: "BLAZE_ROD", weight: 25 },
      { id: "nether_item_4", name: "Nether Item 4", material: "GHAST_TEAR", weight: 25 }
    ],
    experience: 200
  },
  cave: {
    name: "Cave Collection",
    tier: "COMMON",
    biomes: ["DRIPSTONE_CAVES", "LUSH_CAVES", "DEEP_DARK"],
    dimensions: ["NORMAL"],
    minY: -64,
    maxY: 50,
    items: [
      { id: "cave_item_1", name: "Cave Item 1", material: "POINTED_DRIPSTONE", weight: 25 },
      { id: "cave_item_2", name: "Cave Item 2", material: "GLOW_BERRIES", weight: 25 },
      { id: "cave_item_3", name: "Cave Item 3", material: "AMETHYST_SHARD", weight: 25 },
      { id: "cave_item_4", name: "Cave Item 4", material: "SCULK", weight: 25 }
    ],
    experience: 175
  },
  end: {
    name: "End Collection",
    tier: "RARE",
    biomes: ["THE_END", "END_HIGHLANDS", "END_MIDLANDS", "SMALL_END_ISLANDS"],
    dimensions: ["THE_END"],
    minY: 0,
    maxY: 128,
    items: [
      { id: "end_item_1", name: "End Item 1", material: "END_STONE", weight: 25 },
      { id: "end_item_2", name: "End Item 2", material: "CHORUS_FRUIT", weight: 25 },
      { id: "end_item_3", name: "End Item 3", material: "ENDER_PEARL", weight: 25 },
      { id: "end_item_4", name: "End Item 4", material: "SHULKER_SHELL", weight: 25 }
    ],
    experience: 250
  },
  desert: {
    name: "Desert Collection",
    tier: "COMMON",
    biomes: ["DESERT"],
    dimensions: ["NORMAL"],
    minY: 60,
    maxY: 100,
    items: [
      { id: "desert_item_1", name: "Desert Item 1", material: "SAND", weight: 25 },
      { id: "desert_item_2", name: "Desert Item 2", material: "CACTUS", weight: 25 },
      { id: "desert_item_3", name: "Desert Item 3", material: "DEAD_BUSH", weight: 25 },
      { id: "desert_item_4", name: "Desert Item 4", material: "RABBIT_HIDE", weight: 25 }
    ],
    experience: 150
  }
};
```

Add a `loadTemplate(templateName)` function that:
1. Gets the template by name
2. Clears the form with resetForm()
3. Populates form-name, form-tier fields
4. Sets form-biomes with comma-joined biomes
5. Sets dimension checkboxes based on template.dimensions
6. Sets form-min-y and form-max-y
7. Sets form-reward-xp
8. Clears items-container and adds template items using addItemRow(item) for each
9. Generates a unique ID suggestion (template name + timestamp suffix)
10. Shows toast "Template loaded"

```javascript
function loadTemplate(templateName) {
  const template = templates[templateName];
  if (!template) return;

  resetForm();

  // Generate unique ID suggestion
  const timestamp = Date.now().toString(36).slice(-4);
  document.getElementById('form-id').value = templateName + '_' + timestamp;
  document.getElementById('form-name').value = template.name;
  document.getElementById('form-tier').value = template.tier;
  document.getElementById('form-biomes').value = template.biomes.join(', ');

  // Set dimensions
  document.getElementById('dim-normal').checked = template.dimensions.includes('NORMAL');
  document.getElementById('dim-nether').checked = template.dimensions.includes('NETHER');
  document.getElementById('dim-end').checked = template.dimensions.includes('THE_END');

  // Set Y levels
  document.getElementById('form-min-y').value = template.minY;
  document.getElementById('form-max-y').value = template.maxY;

  // Set experience
  document.getElementById('form-reward-xp').value = template.experience;

  // Add items
  template.items.forEach(item => addItemRow(item));

  showToast('Template "' + templateName + '" loaded', 'success');
}
```

Modify `showCreateForm()` to show template selector instead of immediately showing form:
1. Set currentEditId = null
2. Show template selector div
3. Hide form view
</action>
  <verify>
Open browser to web panel, click "New Collection", verify template selector appears with 6 template buttons.
Click a template button, verify form populates with appropriate values.
  </verify>
  <done>
Template selector shows 6 options (forest, ocean, nether, cave, end, desert) plus "Start Blank".
Clicking template populates form with biomes, dimensions, Y-levels, items, and experience.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add template selector HTML and CSS</name>
  <files>src/main/resources/web/index.html, src/main/resources/web/css/admin.css</files>
  <action>
In index.html, add template selector div inside view-form, before the collection-form element:

```html
<!-- Template selector (shown when creating new collection) -->
<div id="template-selector" class="template-selector hidden">
  <h3>Start from Template</h3>
  <p class="template-hint">Choose a template or start with a blank collection</p>
  <div class="template-grid">
    <button type="button" class="template-btn" data-template="forest">
      <span class="template-icon">🌲</span>
      <span class="template-name">Forest</span>
    </button>
    <button type="button" class="template-btn" data-template="ocean">
      <span class="template-icon">🌊</span>
      <span class="template-name">Ocean</span>
    </button>
    <button type="button" class="template-btn" data-template="nether">
      <span class="template-icon">🔥</span>
      <span class="template-name">Nether</span>
    </button>
    <button type="button" class="template-btn" data-template="cave">
      <span class="template-icon">⛏️</span>
      <span class="template-name">Cave</span>
    </button>
    <button type="button" class="template-btn" data-template="end">
      <span class="template-icon">🟣</span>
      <span class="template-name">End</span>
    </button>
    <button type="button" class="template-btn" data-template="desert">
      <span class="template-icon">🏜️</span>
      <span class="template-name">Desert</span>
    </button>
  </div>
  <button type="button" id="template-blank-btn" class="btn-secondary">Start Blank</button>
</div>
```

In admin.css, add template selector styles:

```css
/* ========== Template Selector ========== */
.template-selector {
  background: var(--bg-secondary);
  border-radius: 8px;
  padding: 2rem;
  text-align: center;
  max-width: 500px;
  margin: 0 auto;
}

.template-selector h3 {
  margin: 0 0 0.5rem 0;
  color: var(--accent-color);
}

.template-hint {
  color: var(--text-muted);
  margin-bottom: 1.5rem;
}

.template-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-bottom: 1.5rem;
}

.template-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 1.25rem 0.75rem;
  background: var(--bg-tertiary);
  border: 2px solid var(--border-color);
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s ease;
  color: var(--text-primary);
}

.template-btn:hover {
  background: var(--bg-primary);
  border-color: var(--accent-color);
  transform: translateY(-2px);
}

.template-icon {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.template-name {
  font-weight: 500;
  font-size: 0.9rem;
}

@media (max-width: 480px) {
  .template-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}
```

In app.js, in the DOMContentLoaded event listener, add click handlers for template buttons:

```javascript
// Template button handlers
document.querySelectorAll('.template-btn').forEach(btn => {
  btn.addEventListener('click', function() {
    const templateName = this.dataset.template;
    document.getElementById('template-selector').classList.add('hidden');
    document.getElementById('collection-form').classList.remove('hidden');
    loadTemplate(templateName);
    initItemBrowser();
    initCollectionSortable();
  });
});

// Start blank button
document.getElementById('template-blank-btn').addEventListener('click', function() {
  document.getElementById('template-selector').classList.add('hidden');
  document.getElementById('collection-form').classList.remove('hidden');
  resetForm();
  addItemRow(); // Start with one empty item
  initItemBrowser();
  initCollectionSortable();
});
```

Modify showCreateForm() to show template selector:

```javascript
function showCreateForm() {
  currentEditId = null;
  document.getElementById('form-title').textContent = 'New Collection';
  document.getElementById('form-id').disabled = false;

  // Show template selector, hide form initially
  document.getElementById('template-selector').classList.remove('hidden');
  document.getElementById('collection-form').classList.add('hidden');

  showView('view-form');
}
```

Modify showEditForm() to ensure it hides template selector and shows form:

```javascript
async function showEditForm(id) {
  currentEditId = id;
  document.getElementById('form-title').textContent = 'Edit Collection';
  document.getElementById('form-id').disabled = true;

  // Hide template selector, show form for editing
  document.getElementById('template-selector').classList.add('hidden');
  document.getElementById('collection-form').classList.remove('hidden');

  resetForm();
  // ... rest of existing code
}
```
  </action>
  <verify>
Run `./gradlew shadowJar` to build plugin.
Test in browser: "New Collection" shows template selector with 6 templates.
Select "Forest" template - form appears with forest biomes, Overworld dimension, items.
Select "Nether" template - form appears with nether biomes, Nether dimension.
Click "Start Blank" - form appears empty with one empty item row.
Edit existing collection - template selector does NOT appear, form shows directly.
  </verify>
  <done>
Template selector appears for new collections only.
All 6 templates work and populate appropriate values.
"Start Blank" works for empty new collection.
Edit mode bypasses template selector.
  </done>
</task>

</tasks>

<verification>
1. Build: `./gradlew shadowJar`
2. Start test server: `./gradlew runServer`
3. Open web panel, click "New Collection"
4. Verify 6 template buttons visible with correct icons
5. Click each template, verify form populates with:
   - Forest: FOREST biomes, Overworld, Y 60-100, forest items
   - Ocean: OCEAN biomes, Overworld, Y 20-62, ocean items
   - Nether: NETHER biomes, Nether dimension, Y 30-120, nether items
   - Cave: DRIPSTONE_CAVES etc, Overworld, Y -64-50, cave items
   - End: THE_END etc, End dimension, Y 0-128, end items
   - Desert: DESERT biome, Overworld, Y 60-100, desert items
6. Verify "Start Blank" shows empty form
7. Verify editing existing collection shows form directly (no template selector)
</verification>

<success_criteria>
- VBE-02 satisfied: Admin can start new collection from 6 templates
- Template selector UI is visually consistent with existing admin panel styling
- Templates include appropriate biomes, dimensions, Y-levels, and sample items
- Form ID is auto-generated to prevent collisions
</success_criteria>

<output>
After completion, create `.planning/phases/22-visual-builder-enhancements/22-01-SUMMARY.md`
</output>
