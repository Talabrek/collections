---
phase: 21-visual-builder
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/web/index.html
  - src/main/resources/web/css/admin.css
autonomous: true

must_haves:
  truths:
    - "SortableJS library is loaded from CDN"
    - "Form view shows two-panel layout with items on left, browser on right"
    - "Item browser panel has search input and grid container"
    - "Browser items have visual styling with cursor:grab"
    - "Drag feedback shows ghost and chosen states"
  artifacts:
    - path: "src/main/resources/web/index.html"
      provides: "SortableJS CDN script and two-panel HTML structure"
      contains: "sortablejs"
    - path: "src/main/resources/web/css/admin.css"
      provides: "Visual builder styling"
      contains: "item-browser-panel"
  key_links:
    - from: "index.html"
      to: "SortableJS CDN"
      via: "script src"
      pattern: "cdn.*sortablejs"
    - from: "admin.css"
      to: "SortableJS classes"
      via: "CSS selectors"
      pattern: "sortable-ghost|sortable-chosen"
---

<objective>
Create the two-panel visual builder layout with item browser HTML structure and all CSS styling for the drag-drop interface.

Purpose: Establish the UI foundation for the visual item builder before JavaScript integration.
Output: HTML structure with SortableJS CDN, item browser panel, and complete CSS styling for the visual builder.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-visual-builder/21-RESEARCH.md

# Existing files to modify
@src/main/resources/web/index.html
@src/main/resources/web/css/admin.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SortableJS CDN and Item Browser HTML</name>
  <files>src/main/resources/web/index.html</files>
  <action>
1. Add SortableJS CDN script before app.js in the head or end of body:
```html
<script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.6/Sortable.min.js"></script>
```

2. Modify the Items form-section to use two-panel layout. Replace:
```html
<div class="form-section">
    <h3>Items</h3>
    <div id="items-container"></div>
    <button type="button" id="add-item-btn" class="btn-secondary">+ Add Item</button>
    <span class="error-message" data-field="items"></span>
</div>
```

With:
```html
<div class="form-section items-section">
    <h3>Items</h3>
    <div class="items-layout">
        <div class="collection-items-panel">
            <div id="items-container" class="items-container"></div>
            <button type="button" id="add-item-btn" class="btn-secondary">+ Add Item</button>
            <span class="error-message" data-field="items"></span>
        </div>
        <div class="item-browser-panel">
            <div class="browser-header">
                <h4>Item Browser</h4>
                <span id="browser-count" class="browser-count"></span>
            </div>
            <div class="browser-search">
                <input type="text" id="item-search" placeholder="Search items..." autocomplete="off">
            </div>
            <div id="item-browser-grid" class="item-browser-grid">
                <p class="loading">Loading items...</p>
            </div>
        </div>
    </div>
</div>
```

3. Update item-row structure to include drag handle. The existing addItemRow() in app.js creates item rows dynamically. The CSS will style the existing .item-row-header to include a drag handle appearance. No HTML changes needed here as the structure is generated by JavaScript.
  </action>
  <verify>
Open index.html in browser, verify:
- SortableJS script loads without errors (check network tab)
- Items section shows two-panel layout structure
- Browser panel has search input and grid container
  </verify>
  <done>
SortableJS CDN loaded, two-panel layout structure in place with collection items on left, item browser on right.
  </done>
</task>

<task type="auto">
  <name>Task 2: Visual Builder CSS Styling</name>
  <files>src/main/resources/web/css/admin.css</files>
  <action>
Add comprehensive CSS for the visual builder. Add these rules to admin.css:

```css
/* ========== Visual Builder Layout ========== */

/* Two-panel layout for items section */
.items-section .items-layout {
    display: flex;
    gap: 1rem;
}

.collection-items-panel {
    flex: 2;
    min-width: 300px;
}

.item-browser-panel {
    flex: 1;
    min-width: 250px;
    max-width: 350px;
    background: var(--bg-tertiary);
    border-radius: 8px;
    padding: 1rem;
    position: sticky;
    top: 1rem;
    max-height: calc(100vh - 4rem);
    display: flex;
    flex-direction: column;
}

.browser-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.75rem;
}

.browser-header h4 {
    margin: 0;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
}

.browser-count {
    font-size: 0.8rem;
    color: var(--text-muted);
}

.browser-search {
    margin-bottom: 0.75rem;
}

.browser-search input {
    width: 100%;
    padding: 0.5rem 0.75rem;
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 4px;
    color: var(--text-primary);
    font-size: 0.9rem;
}

.browser-search input:focus {
    outline: none;
    border-color: var(--accent-color);
}

/* Item browser grid */
.item-browser-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(56px, 1fr));
    gap: 4px;
    overflow-y: auto;
    flex: 1;
    padding: 4px;
}

.browser-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 6px 4px;
    cursor: grab;
    border-radius: 4px;
    background: var(--bg-secondary);
    border: 1px solid transparent;
    transition: all 0.15s ease;
    min-height: 56px;
}

.browser-item:hover {
    background: var(--bg-primary);
    border-color: var(--accent-color);
    transform: scale(1.05);
}

.browser-item:active {
    cursor: grabbing;
}

.browser-item .item-icon {
    font-size: 1.5rem;
    margin-bottom: 2px;
}

.browser-item .item-label {
    font-size: 0.65rem;
    color: var(--text-muted);
    text-align: center;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    max-width: 100%;
}

/* Items container styling */
.items-container {
    min-height: 100px;
    border: 2px dashed var(--border-color);
    border-radius: 8px;
    padding: 0.5rem;
    margin-bottom: 0.75rem;
    transition: all 0.2s ease;
}

.items-container:empty::before {
    content: 'Drag items here or click Add Item';
    display: block;
    text-align: center;
    padding: 2rem;
    color: var(--text-muted);
    font-style: italic;
}

.items-container.drag-over {
    background: rgba(0, 217, 255, 0.05);
    border-color: var(--accent-color);
}

/* Drag handle for item rows */
.item-row {
    position: relative;
}

.item-row-header {
    display: flex;
    align-items: center;
    padding: 0.5rem 0.75rem;
    background: var(--bg-secondary);
    border-bottom: 1px solid var(--border-color);
    border-radius: 6px 6px 0 0;
}

.drag-handle {
    cursor: grab;
    padding: 0 0.5rem 0 0;
    color: var(--text-muted);
    font-size: 1rem;
    user-select: none;
}

.drag-handle:active {
    cursor: grabbing;
}

.item-number {
    flex: 1;
    font-weight: 500;
}

/* SortableJS drag states */
.sortable-ghost {
    opacity: 0.4;
    background: var(--accent-color) !important;
}

.sortable-chosen {
    box-shadow: 0 4px 12px rgba(0, 217, 255, 0.3);
}

.sortable-drag {
    opacity: 1 !important;
}

/* Body class during drag for global feedback */
body.is-dragging .items-container {
    background: rgba(0, 217, 255, 0.03);
}

body.is-dragging .item-browser-grid .browser-item {
    pointer-events: none;
}

/* Checkbox row for dimensions */
.checkbox-row {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
}

.checkbox-row label {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    cursor: pointer;
    font-weight: normal;
}

/* Responsive: Stack panels on narrow screens */
@media (max-width: 768px) {
    .items-section .items-layout {
        flex-direction: column;
    }

    .item-browser-panel {
        max-width: none;
        max-height: 300px;
        position: static;
    }
}
```
  </action>
  <verify>
Open web panel, navigate to form view.
Verify:
- Two-panel layout visible (items left, browser right)
- Browser panel has proper styling with rounded corners
- Search input styled correctly
- Grid area scrolls independently
- Hover states work on browser items
- Items container shows dashed border with placeholder text
  </verify>
  <done>
Complete CSS styling for visual builder including two-panel layout, browser grid, drag handles, SortableJS states, and responsive breakpoints.
  </done>
</task>

</tasks>

<verification>
1. SortableJS loads from CDN without console errors
2. Form view shows two-panel layout (collection items | item browser)
3. Item browser panel has search input and grid container
4. CSS provides proper styling for browser items, drag states, responsive layout
5. Items container shows placeholder text when empty
</verification>

<success_criteria>
- SortableJS 1.15.6 loaded from CDN (VB-03, VB-04 dependency)
- Two-panel layout renders correctly (VB-02)
- Browser panel has search and grid structure ready for JavaScript
- All visual states styled (hover, drag ghost, chosen)
- Responsive layout works on narrow screens
</success_criteria>

<output>
After completion, create `.planning/phases/21-visual-builder/21-02-SUMMARY.md`
</output>
