---
phase: 09-testing-verification
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/test/java/com/blockworlds/collections/model/SpawnConditionsTest.java
autonomous: true

must_haves:
  truths:
    - "SpawnConditions validation methods tested"
    - "Y range validation tested with edge cases"
    - "Light range validation tested with edge cases"
    - "Builder pattern tested"
    - "NONE constant behavior tested"
    - "mergeWith logic tested"
  artifacts:
    - path: "src/test/java/com/blockworlds/collections/model/SpawnConditionsTest.java"
      provides: "SpawnConditions unit tests"
      min_lines: 100
  key_links:
    - from: "SpawnConditionsTest"
      to: "SpawnConditions"
      via: "JUnit 5 test methods"
      pattern: "@Test"
---

<objective>
Add comprehensive unit tests for SpawnConditions validation methods.

Purpose: TEST-01 requires testing SpawnConditions validation. These are pure unit tests that don't require MockBukkit - they test isYValid(), isLightValid(), builder pattern, NONE constant, and mergeWith() logic.

Output: New SpawnConditionsTest.java with comprehensive coverage of validation methods.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-testing-verification/09-RESEARCH.md
@src/main/java/com/blockworlds/collections/model/SpawnConditions.java
@src/test/java/com/blockworlds/collections/model/PlayerProgressTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SpawnConditionsTest with pure validation tests</name>
  <files>src/test/java/com/blockworlds/collections/model/SpawnConditionsTest.java</files>
  <action>
Create `src/test/java/com/blockworlds/collections/model/SpawnConditionsTest.java` following the existing test patterns (see PlayerProgressTest.java).

Test cases to include:

**NONE constant tests:**
- `testNoneHasNoRestrictions()` - NONE accepts any Y, any light, null biomes/dimensions

**isYValid() tests:**
- `testIsYValidWithinRange()` - Y within min-max returns true
- `testIsYValidAtBoundaries()` - Y exactly at minY and maxY returns true
- `testIsYValidOutsideRange()` - Y below min or above max returns false
- `testIsYValidWithDefaultRange()` - MIN_VALUE to MAX_VALUE accepts everything

**isLightValid() tests:**
- `testIsLightValidWithinRange()` - Light within 0-15 range returns true
- `testIsLightValidAtBoundaries()` - Light exactly at min/max returns true
- `testIsLightValidOutsideRange()` - Light outside range returns false

**Builder tests:**
- `testBuilderCreatesCorrectValues()` - All fields set via builder match getters
- `testBuilderDefaults()` - Unset fields have correct defaults

**mergeWith() tests:**
- `testMergeWithNull()` - mergeWith(null) returns this unchanged
- `testMergeWithOverridesBiomes()` - other.biomes replaces this.biomes when set
- `testMergeWithOverridesDimensions()` - other.dimensions replaces when set
- `testMergeWithOverridesYRange()` - non-default Y range from other used
- `testMergeWithKeepsBaseWhenOtherDefault()` - default values in other don't override
- `testMergeWithCombinesFlags()` - requireSky/underground OR'd together
- `testMergeWithOverridesTime()` - non-ALWAYS time from other used

Use JUnit 5 with @Test and @DisplayName annotations. No MockBukkit needed.

```java
package com.blockworlds.collections.model;

import org.bukkit.World;
import org.bukkit.block.Biome;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import java.util.Set;

import static org.junit.jupiter.api.Assertions.*;
```
  </action>
  <verify>Run `./gradlew test --tests SpawnConditionsTest` - all tests pass</verify>
  <done>SpawnConditionsTest.java exists with 15+ test methods covering all validation logic, all tests pass</done>
</task>

</tasks>

<verification>
1. `src/test/java/com/blockworlds/collections/model/SpawnConditionsTest.java` exists
2. Test file has 15+ test methods
3. `./gradlew test --tests SpawnConditionsTest` shows all tests passing
4. No MockBukkit dependency (pure unit tests)
</verification>

<success_criteria>
- SpawnConditionsTest.java created with comprehensive tests
- All NONE constant behavior tested
- All isYValid() edge cases tested
- All isLightValid() edge cases tested
- All builder pattern tested
- All mergeWith() logic tested
- Test suite passes: `./gradlew test --tests SpawnConditionsTest`
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-verification/09-02-SUMMARY.md`
</output>
