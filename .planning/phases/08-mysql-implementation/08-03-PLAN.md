---
phase: 08-mysql-implementation
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/main/resources/config.yml
autonomous: true

must_haves:
  truths:
    - "Config.yml documents all MySQL settings with comments"
    - "MySQL section includes all connection parameters"
    - "Comments explain when to use MySQL vs SQLite"
  artifacts:
    - path: "src/main/resources/config.yml"
      provides: "MySQL configuration documentation"
      contains: "Multi-server networks should use mysql"
  key_links: []
---

<objective>
Enhance config.yml documentation for MySQL settings.

Purpose: Provide clear guidance on when and how to use MySQL for multi-server deployments. Operators need to understand the use case and all configuration options.

Output: Updated config.yml with comprehensive MySQL documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-mysql-implementation/08-RESEARCH.md

@src/main/resources/config.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance config.yml database section</name>
  <files>src/main/resources/config.yml</files>
  <action>
Update the database section in config.yml with comprehensive documentation:

**Replace the existing database section (lines 68-83) with:**

```yaml
# Database Settings
# Collections supports two storage backends:
#   - sqlite: Single-server deployments (default, no setup required)
#   - mysql: Multi-server networks (shared database across servers)
#
# MIGRATION NOTE: SQLite and MySQL use separate databases.
# If switching from SQLite to MySQL, player progress will NOT transfer automatically.
# You must manually export/import data or have players re-collect.
database:
  # Database type: sqlite or mysql
  # Use 'sqlite' for single-server setups (simpler, no external database needed)
  # Use 'mysql' for multi-server networks (shared progress across servers)
  type: sqlite

  # SQLite settings (only used if type is sqlite)
  # Path is relative to server root directory
  sqlite:
    path: plugins/Collections/collections.db

  # MySQL settings (only used if type is mysql)
  # Requires MySQL 8.0+ server
  mysql:
    host: localhost
    port: 3306
    database: collections
    username: root
    password: ""
    # Connection pool size - increase for high-traffic networks
    # Recommended: 10 for single server, 5-10 per server in a network
    pool-size: 10
```

**Also update the old `path` reference** - if there's a standalone `path` under database, remove it (it's now under `sqlite:`)
  </action>
  <verify>
1. config.yml contains "Multi-server networks" text
2. config.yml has separate sqlite: and mysql: sections
3. config.yml compiles/parses correctly (no YAML syntax errors)
  </verify>
  <done>
Config.yml has comprehensive MySQL documentation with migration note and pool-size guidance
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ConfigManager to read new sqlite path</name>
  <files>src/main/java/com/blockworlds/collections/config/ConfigManager.java</files>
  <action>
Update ConfigManager.reload() to read the new config path:

**Change (around line 96):**
```java
// OLD:
databasePath = config.getString("database.path", "plugins/Collections/data.db");

// NEW:
databasePath = config.getString("database.sqlite.path", "plugins/Collections/collections.db");
```

This maintains backward compatibility (getString returns default if path doesn't exist).
  </action>
  <verify>
`./gradlew compileJava` passes
  </verify>
  <done>
ConfigManager reads database.sqlite.path for SQLite database location
  </done>
</task>

<task type="auto">
  <name>Task 3: Update SQLiteStorage to use ConfigManager path</name>
  <files>src/main/java/com/blockworlds/collections/storage/SQLiteStorage.java</files>
  <action>
Update SQLiteStorage.initialize() to use ConfigManager for the database path:

**In initialize() method, update the dbFile logic:**

```java
@Override
public void initialize() {
    File dbFile;
    if (databasePath != null) {
        // Custom path from test constructor
        dbFile = new File(databasePath);
    } else {
        // Get path from config
        String configPath = plugin.getConfigManager().getDatabasePath();
        if (configPath.startsWith("plugins/")) {
            // Relative to server root
            dbFile = new File(configPath);
        } else {
            // Relative to plugin data folder
            File dataFolder = plugin.getDataFolder();
            if (!dataFolder.exists()) {
                dataFolder.mkdirs();
            }
            dbFile = new File(dataFolder, configPath);
        }
    }
    // ... rest of method unchanged
```

This allows config.yml to specify the database path instead of hardcoding it.
  </action>
  <verify>
`./gradlew compileJava` passes
  </verify>
  <done>
SQLiteStorage uses configurable database path from config.yml
  </done>
</task>

</tasks>

<verification>
1. config.yml has comprehensive MySQL documentation
2. ConfigManager reads from database.sqlite.path
3. SQLiteStorage uses ConfigManager for database path
4. `./gradlew shadowJar` builds successfully
</verification>

<success_criteria>
- Config.yml clearly documents MySQL vs SQLite use cases
- Migration limitation is documented (no automatic transfer)
- Pool-size guidance provided for network deployments
- SQLite path configurable via config
</success_criteria>

<output>
After completion, create `.planning/phases/08-mysql-implementation/08-03-SUMMARY.md`
</output>
