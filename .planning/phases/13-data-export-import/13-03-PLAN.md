---
phase: 13-data-export-import
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - src/test/java/com/blockworlds/collections/manager/DataMigrationManagerTest.java
autonomous: true

must_haves:
  truths:
    - "Export produces valid JSON matching expected format"
    - "Import correctly parses exported JSON"
    - "Validation catches malformed JSON and unsupported versions"
    - "Dry-run mode does not modify storage"
    - "Cache invalidation called for online players after import"
  artifacts:
    - path: "src/test/java/com/blockworlds/collections/manager/DataMigrationManagerTest.java"
      provides: "Unit tests for DataMigrationManager"
      min_lines: 150
  key_links:
    - from: "DataMigrationManagerTest.java"
      to: "DataMigrationManager"
      via: "unit tests with mocked dependencies"
      pattern: "DataMigrationManager"
---

<objective>
Create comprehensive unit tests for DataMigrationManager covering export, import, validation, and cache invalidation.

Purpose: Ensure export/import functionality is reliable and handles edge cases correctly.

Output: DataMigrationManagerTest with tests for all export/import scenarios.
</objective>

<execution_context>
@.claude/workflows/execute-plan.md
@.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-data-export-import/13-01-SUMMARY.md
@.planning/phases/13-data-export-import/13-02-SUMMARY.md
@src/main/java/com/blockworlds/collections/manager/DataMigrationManager.java
@src/main/java/com/blockworlds/collections/model/ExportFormat.java
@src/main/java/com/blockworlds/collections/model/ValidationResult.java
@src/main/java/com/blockworlds/collections/model/ImportResult.java
@src/test/java/com/blockworlds/collections/manager/PlayerDataManagerTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataMigrationManagerTest with export tests</name>
  <files>
    src/test/java/com/blockworlds/collections/manager/DataMigrationManagerTest.java
  </files>
  <action>
Create DataMigrationManagerTest.java following existing test patterns (see PlayerDataManagerTest):

1. Test setup:
   - Use Mockito to mock: Collections plugin, Storage, PlayerDataManager, CommandSender
   - Create temp directory for export files using @TempDir
   - Initialize DataMigrationManager with mocks
   - Mock plugin.getDataFolder() to return temp directory
   - Mock plugin.getDescription().getVersion() to return "1.0.0-TEST"

2. Export tests:

   a. `testExportSinglePlayer_createsValidJson()`:
      - Mock storage.loadPlayer() to return PlayerProgress with test data
      - Call exportPlayer()
      - Verify JSON file created in exports/ subdirectory
      - Parse JSON and verify structure: formatVersion, exportDate, pluginVersion, players array
      - Verify player data matches mock

   b. `testExportSinglePlayer_includesAllCollections()`:
      - Create PlayerProgress with multiple collections (some complete, some partial)
      - Export and verify all collections present in JSON
      - Verify items, complete flag, rewardClaimed flag preserved

   c. `testExportAllPlayers_streamingDoesNotLoadAllAtOnce()`:
      - Mock storage.getAllPlayerUuids() to return list of 5 UUIDs
      - Mock storage.loadPlayer() for each UUID
      - Call exportAllPlayers()
      - Verify loadPlayer called once per UUID (streaming, not batch)
      - Verify JSON contains all 5 players

   d. `testExportAllPlayers_createsExportsDirectoryIfMissing()`:
      - Ensure exports/ doesn't exist
      - Call exportAllPlayers()
      - Verify exports/ directory created
      - Verify file exists

   e. `testExportSinglePlayer_logsAdminAction()`:
      - Mock plugin.getLogger()
      - Call exportPlayer()
      - Verify logger.info() called with message containing "EXPORT"
  </action>
  <verify>
    `./gradlew test --tests DataMigrationManagerTest` - all export tests pass
  </verify>
  <done>
    Export unit tests created and passing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add validation and import tests</name>
  <files>
    src/test/java/com/blockworlds/collections/manager/DataMigrationManagerTest.java
  </files>
  <action>
Add validation and import tests to DataMigrationManagerTest:

1. Validation tests:

   a. `testValidateImportFile_validFile_returnsSuccess()`:
      - Create valid JSON file with correct format
      - Call validateImportFile()
      - Assert isValid() returns true
      - Assert playerCount matches

   b. `testValidateImportFile_missingFormatVersion_returnsError()`:
      - Create JSON without formatVersion field
      - Call validateImportFile()
      - Assert isValid() returns false
      - Assert errors contain "formatVersion"

   c. `testValidateImportFile_unsupportedVersion_returnsError()`:
      - Create JSON with formatVersion: 999
      - Call validateImportFile()
      - Assert isValid() returns false
      - Assert errors contain "version"

   d. `testValidateImportFile_malformedJson_returnsError()`:
      - Create file with invalid JSON syntax
      - Call validateImportFile()
      - Assert isValid() returns false
      - Assert errors contain parse error

   e. `testValidateImportFile_missingPlayersArray_returnsError()`:
      - Create JSON without players field
      - Call validateImportFile()
      - Assert isValid() returns false

   f. `testValidateImportFile_invalidPlayerEntry_returnsError()`:
      - Create JSON with player missing uuid
      - Call validateImportFile()
      - Assert isValid() returns false

2. Import tests:

   a. `testImportPlayers_dryRun_doesNotModifyStorage()`:
      - Create valid export JSON
      - Call importPlayers(path, dryRun=true, sender)
      - Verify storage.savePlayer() NEVER called
      - Verify result contains correct player count

   b. `testImportPlayers_savesAllPlayers()`:
      - Create JSON with 3 players
      - Call importPlayers(path, dryRun=false, sender)
      - Verify storage.savePlayer() called 3 times
      - Verify result.playersImported() == 3

   c. `testImportPlayers_invalidFile_returnsFailure()`:
      - Create invalid JSON file
      - Call importPlayers()
      - Assert result.success() is false
      - Verify storage.savePlayer() never called

   d. `testImportPlayers_nonExistentFile_returnsFailure()`:
      - Call importPlayers with path to non-existent file
      - Assert result.success() is false
      - Assert errorMessage contains file not found
  </action>
  <verify>
    `./gradlew test --tests DataMigrationManagerTest` - all tests pass
  </verify>
  <done>
    Validation and import tests added and passing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add cache invalidation tests and edge cases</name>
  <files>
    src/test/java/com/blockworlds/collections/manager/DataMigrationManagerTest.java
  </files>
  <action>
Add cache invalidation and edge case tests:

1. Cache invalidation tests:

   a. `testImportPlayers_invalidatesCacheForOnlinePlayers()`:
      - Create JSON with 2 players
      - Mock Bukkit.getPlayer() to return Player for one UUID, null for other
      - Call importPlayers(path, dryRun=false, sender)
      - Verify playerDataManager.invalidateCacheAndReload() called once (for online player)
      - Verify result.affectedOnlinePlayers() contains online player's UUID

   b. `testImportPlayers_dryRun_identifiesOnlinePlayers()`:
      - Create JSON with 3 players
      - Mock 2 as online
      - Call importPlayers(dryRun=true)
      - Verify result.affectedOnlinePlayers().size() == 2
      - Verify invalidateCacheAndReload() NOT called

2. Edge case tests:

   a. `testExportEmptyDatabase_createsValidJson()`:
      - Mock storage.getAllPlayerUuids() to return empty list
      - Call exportAllPlayers()
      - Verify JSON created with empty players array
      - Verify totalPlayers: 0

   b. `testImportEmptyPlayersArray_succeeds()`:
      - Create valid JSON with empty players array
      - Call importPlayers()
      - Assert result.success() is true
      - Assert result.playersImported() == 0

   c. `testExportPlayer_notFound_returnsFailure()`:
      - Mock storage.loadPlayer() to return new empty PlayerProgress
      - Call exportPlayer() with UUID that has no data
      - Verify export still succeeds (exports empty progress)

   d. `testValidateImportFile_emptyFile_returnsError()`:
      - Create empty file
      - Call validateImportFile()
      - Assert isValid() returns false

3. Round-trip test:

   a. `testExportThenImport_preservesAllData()`:
      - Create PlayerProgress with complex data:
        - Multiple collections
        - Some complete, some partial
        - Various stats
      - Export to file
      - Clear mock/reset
      - Import from same file
      - Capture saved PlayerProgress
      - Verify all data matches original
  </action>
  <verify>
    `./gradlew test --tests DataMigrationManagerTest` - all tests pass
  </verify>
  <done>
    Cache invalidation and edge case tests complete. Full test coverage for DataMigrationManager.
  </done>
</task>

</tasks>

<verification>
1. All tests pass: `./gradlew test --tests DataMigrationManagerTest`
2. Export tests verify JSON format and content
3. Validation tests cover all error cases
4. Import tests verify storage interactions
5. Dry-run tests confirm no storage modifications
6. Cache invalidation tests verify online player handling
7. Round-trip test confirms data integrity
</verification>

<success_criteria>
1. DataMigrationManagerTest exists with comprehensive test coverage
2. Export functionality tested for single and all players
3. Validation tested for all error conditions
4. Import tested for success and failure cases
5. Dry-run mode tested to ensure no side effects
6. Cache invalidation tested for online player scenarios
7. All tests pass with `./gradlew test`
</success_criteria>

<output>
After completion, create `.planning/phases/13-data-export-import/13-03-SUMMARY.md`
</output>
