# Milestone v1.1: Operational Features

**Status:** ✅ SHIPPED 2026-01-22
**Phases:** 10-13
**Total Plans:** 14

## Overview

Add operator tooling, player feedback, and observability features for production network deployment. This milestone provides real-time feedback when players collect items, admin commands for managing player progress, community metrics via bStats, PlaceholderAPI integration for stat displays, and data export/import for server migration.

## Phases

### Phase 10: Progress Notifications

**Goal**: Players receive immediate visual and audio feedback when collecting items and completing collections.
**Depends on**: None (builds on existing ItemUseListener)
**Requirements**: NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05
**Plans**: 3 plans

Plans:
- [x] 10-01-PLAN.md — NotificationManager + config structure
- [x] 10-02-PLAN.md — Integration into ConfirmAddGUI
- [x] 10-03-PLAN.md — Unit tests for notification system

**Success Criteria:**
1. Player sees actionbar progress (e.g., "2/5 Forest Collection") when collecting a new item
2. Player sees title announcement and hears sound when completing any collection
3. Server admin can change notification style (actionbar/chat/title) via config.yml without restart
4. Rapidly collecting the same item does not flood the player with duplicate messages

### Phase 11: Admin Commands

**Goal**: Server admins can inspect and modify any player's collection progress through in-game commands.
**Depends on**: None (uses existing PlayerDataManager)
**Requirements**: ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05
**Plans**: 3 plans

Plans:
- [x] 11-01-PLAN.md — PlayerDataManager offline player support
- [x] 11-02-PLAN.md — Admin inspect and complete commands
- [x] 11-03-PLAN.md — Unit tests for admin operations

**Success Criteria:**
1. Admin can run `/collections admin complete <player> <collection>` to mark a collection done
2. Admin can inspect any player's progress with `/collections admin inspect <player>` showing completion percentages
3. Commands work for offline players using name or UUID
4. Every admin action appears in server log with timestamp, executor name, and affected player

### Phase 12: Metrics & Observability

**Goal**: Server operators can monitor plugin activity via bStats dashboard and players can display stats via PlaceholderAPI.
**Depends on**: Phase 10 (notification events feed counters), Phase 11 (admin actions feed counters)
**Requirements**: METRICS-01, METRICS-02, METRICS-03, METRICS-04, METRICS-05, METRICS-06
**Plans**: 5 plans

Plans:
- [x] 12-01-PLAN.md — MetricsManager + bStats core + build config
- [x] 12-02-PLAN.md — Hook counter increments into existing code
- [x] 12-03-PLAN.md — PlaceholderAPI expansion class
- [x] 12-04-PLAN.md — Counter persistence to database
- [x] 12-05-PLAN.md — Unit tests for MetricsManager

**Success Criteria:**
1. Plugin appears on bStats.org with server count and custom charts after server restart
2. Internal counters track items collected, collections completed, and spawn success/failure rates
3. Player can use `%collections_completed%` placeholder in chat plugins showing their completion count
4. Server can use `%collections_server_total%` placeholder showing server-wide statistics
5. Counter values persist across server restarts

### Phase 13: Data Export/Import

**Goal**: Server admins can export player data for backup/migration and import data with validation.
**Depends on**: Phase 11 (shares admin permission structure)
**Requirements**: EXPORT-01, EXPORT-02, EXPORT-03, EXPORT-04, EXPORT-05, EXPORT-06
**Plans**: 3 plans

Plans:
- [x] 13-01-PLAN.md — DataMigrationManager + streaming export (EXPORT-01, EXPORT-02)
- [x] 13-02-PLAN.md — Import with validation, dry-run, cache invalidation (EXPORT-03..06)
- [x] 13-03-PLAN.md — Unit tests for DataMigrationManager

**Success Criteria:**
1. Admin can export single player or all players to JSON file in plugins/Collections/exports/
2. Export of large datasets (10k+ players) completes without OutOfMemoryError using streaming
3. Admin can dry-run import to see what would change before committing
4. Online players see their updated progress immediately after import without rejoin

## Progress Summary

| Phase | Plans Complete | Completed |
|-------|----------------|-----------|
| 10. Progress Notifications | 3/3 | 2026-01-22 |
| 11. Admin Commands | 3/3 | 2026-01-22 |
| 12. Metrics & Observability | 5/5 | 2026-01-22 |
| 13. Data Export/Import | 3/3 | 2026-01-22 |

---

## Milestone Summary

**Key Decisions:**
- Progress notifications default to actionbar for non-intrusive feedback
- Completion notifications default to title for celebratory impact
- Offline player data NOT cached to avoid memory leaks (load-modify-save pattern)
- Admin audit log at INFO level (plugin logger adds timestamps)
- bStats relocated to avoid plugin conflicts
- AtomicLong counters for thread-safe async access
- Gson as compileOnly (Paper bundles at runtime)
- JsonWriter streaming to avoid OOM on large datasets
- Validation-first import pattern (parse twice: validate, then import)

**Issues Resolved:**
- No duplicate notifications on rapid collecting (built into NotificationManager)
- Offline player operations now supported for admin commands
- Large export datasets no longer cause OutOfMemoryError
- Import cache invalidation ensures online players see updated data

**Issues Deferred:**
- MockBukkit IncompatibleClassChangeError (pre-existing, requires MockBukkit update)
- Flaky test testAddItemOffline_UsesCache (race condition in test, not implementation)

**Technical Debt Incurred:**
- None identified during milestone

---

*Archived: 2026-01-22 as part of v1.1 milestone completion*
*For current project status, see .planning/ROADMAP.md*
